v = [sin(pi/4) cos(pi/3) log(2) sqrt(exp)]

%% 1.1
v_vic=round(v); % approssimo all'intero più "vicino"

%% 1.2
v_sup=ceil(v); % approssimo all'intero superiore

%% 1.3
v_inf=floor(v); % approssimo all'intero inferiore

%% 1.4
v_col=v'